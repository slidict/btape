services:
  app:
    build: .
    working_dir: /app
    volumes:
      - .:/app
      - bundle:/bundle
    environment:
      CHROME_PATH: /usr/bin/chromium
      FERRUM_CHROME_DOCKERIZE: "true"

volumes:
  bundle:

